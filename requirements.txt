# ── Core AWS SDK ──────────────────────────────────────────────────────────────
boto3>=1.34.0
botocore>=1.34.0

# ── PDF Extraction ────────────────────────────────────────────────────────────
PyMuPDF>=1.24.0               # Primary extractor — resolves Arabic font encoding via ToUnicode CMap

# ── Docling (deep document structuring) ──────────────────────────────────────
# Used as an asynchronous post-processing pass on EC2 (g4dn.xlarge recommended)
# for rich structural output: tables, cross-references, heading hierarchy.
# Arabic support is experimental — validate on reference PDFs before production.
docling>=2.0.0
docling-core>=2.0.0

# ── Database / ORM ───────────────────────────────────────────────────────────
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9        # PostgreSQL driver for Amazon RDS

# ── Arabic NLP & Text Processing ─────────────────────────────────────────────
pyarabic>=0.6.15              # Arabic text normalisation, tashkeel removal
arabic-reshaper>=3.0.0        # Reshape Arabic for correct rendering
python-bidi>=0.4.2            # BiDi algorithm for RTL text handling
unicodedata2>=15.1.0          # Extended Unicode normalisation

# ── Configuration & Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0          # Load environment variables from .env
pydantic>=2.5.0               # Data validation and settings management
pydantic-settings>=2.1.0
tenacity>=8.2.3               # Retry logic for AWS API calls
tqdm>=4.66.0                  # Progress bars for batch processing
rich>=13.7.0                  # Pretty console output / logging

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-mock>=3.12.0
moto>=5.0.0                   # AWS service mocking for unit tests
